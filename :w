.wrapper{
    display:flex;
    width: calc(var(--card-width));
    height: calc(var(--card-height) );
    margin: 2px;
    border: 3px solid white;
    border-radius: 15px;
    background-color: green;
    cursor: pointer;
}

.stack-span{
    place-self: center;
    color: white;
    font-size: 4em;
    width: 100%;
}
